{
  "name": "root",
  "scripts": {
    "start": "lerna bootstrap && lerna run build && concurrently \"lerna run watch --stream\" \"lerna run start --stream\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "lerna": "^3.4.3"
  }
}
