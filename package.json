{
  "name": "vizhub-io",
  "scripts": {
    "start": "lerna bootstrap && concurrently \"lerna run build-watch --stream\" \"lerna run start-watch --stream\" \"lerna run test-watch --stream\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "lerna": "^3.4.3"
  }
}
